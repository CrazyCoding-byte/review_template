spring:
  # 数据源配置（原位置不变）
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/transaction_demo?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    password: yangzhixuan
    username: root
# RocketMQ 配置（和 spring 同级，原配置不变）
rocketmq:
  name-server: 192.168.1.13:9876
  producer:
    group: test_pay_producer_group
    send-message-timeout: 3000
    compress-message-body-threshold: 4096
    max-message-size: 4194304
server:
  port: 8081